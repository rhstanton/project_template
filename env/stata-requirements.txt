# Stata package versions vendored in .stata/ado/plus, which is committed.
# Generated by 'make stata-requirements' using require's own list mode.
#
# Check the installed tree against this file:  make stata-check
#
# SSC serves only the current release, so these cannot be re-obtained by
# version; that is why the ado files themselves are committed.
coefplot == 1.8.8
estout == 3.33.0
ftools == 2.50.0
reghdfe == 6.13.1
require == 1.3.1
